
let config = {};

config.port = 6008;

config.brand = '#BlondieCode';

config.error = {
    notFound: 'Нет такой страницы =(',
    serverError: 'Ошибка сервера'
};

config.directories = {
    readyFiles: './ready'
};

module.exports = config;